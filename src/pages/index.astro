---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.tsx';
import Hero from '../components/Hero.tsx';
import DemoSection from '../components/DemoSection.tsx';
import ComparisonSection from '../components/ComparisonSection.tsx';
import WhyTau from '../components/WhyTau.tsx';
import Testimonials from '../components/Testimonials.tsx';
import Features from '../components/Features.tsx';
import Pricing from '../components/Pricing.tsx';
import FAQ from '../components/FAQ.tsx';
import Footer from '../components/Footer.tsx';
import { faqs } from '../data/faqs';
import { getPricingTier } from '../lib/redis';
import PromoBanner from '../components/PromoBanner.tsx';

const { solo, spotsLeft, tierIndex } = await getPricingTier();

const TIER_NEXT_PRICES: Record<number, { solo: string; pro: string } | null> = {
	1: { solo: '€18.99', pro: '€40.99' },
	2: { solo: '€24.99', pro: '€50.99' },
	3: null,
};
const nextPrice = TIER_NEXT_PRICES[tierIndex]?.solo ?? null;

const keywords = [
	// Core product
	'timelapse app', 'timelapse software', 'timelapse recorder', 'timelapse editor',
	'timelapse recording software', 'timelapse recording app', 'timelapse creator', 'timelapse maker',
	// Platform-specific (high intent)
	'timelapse app for mac', 'timelapse app for windows', 'best timelapse app',
	'timelapse software for mac', 'timelapse software for windows',
	'how to record timelapse on mac', 'how to make a timelapse on mac',
	// Screen recording
	'screen timelapse', 'screen recording timelapse', 'screen timelapse recorder',
	'record screen as timelapse', 'screen capture timelapse', 'how to make a screen timelapse',
	'screen recorder mac', 'screen recorder windows',
	// Multi-source
	'screen and camera recording', 'record screen and webcam', 'screen and webcam timelapse',
	'multi source timelapse recorder',
	// Use-case / audience (coding, design, creative)
	'coding timelapse', 'programming timelapse', 'coding session recorder',
	'design process timelapse', 'art timelapse', 'digital art timelapse',
	'devlog timelapse', 'workflow timelapse',
	// Feature-specific
	'timelapse with overlays', 'timelapse with timer overlay',
	'video timelapse', 'timelapse video',
];
---

<Layout
	title="Tau — Screen & Camera Timelapse Recorder for Mac & Windows"
	description="Record your screen, camera, or both at once — then edit and export stunning timelapses on macOS and Windows. Smart overlays, multi-source recording, and one-click export."
	keywords={keywords}
	jsonLdSchema="software"
	faqItems={faqs}
>
	<Navbar />
	<PromoBanner currentPrice={solo} nextPrice={nextPrice} spotsLeft={spotsLeft} />
	<Hero spotsLeft={spotsLeft} nextPrice={nextPrice} />
	<DemoSection client:load />
	<WhyTau />
	<Features />
	<ComparisonSection client:load />
	<Testimonials client:load />
	<Pricing client:load />
	<FAQ />
	<Footer />
</Layout>
